<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_prj">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Androidprj">

        <activity android:name=".HeuristicRuleBasesAI"
            android:exported="true" ></activity>
        <activity
            android:name=".HorsLigne_menu"
            android:exported="true" />

        <activity
            android:name=".EnLigne_menu"
            android:exported="true" />

        <activity android:name=".MiniMaxActivity"
            android:exported="true"></activity>

        <activity android:name=".Credits" />
        <activity android:name=".HowToPlay" />
        <activity android:name=".Dashboard" />

        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>